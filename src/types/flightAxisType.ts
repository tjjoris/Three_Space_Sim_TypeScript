/**
 *flightAxisType.ts
 @Author: Luke Johnson
 A type which can only be one of the following strings.
 used for joys and bindings types.
 */
export type FlightAxisType = "pitch" | "roll" | "yaw" | "horizontal" | "vertical" | "forward";
